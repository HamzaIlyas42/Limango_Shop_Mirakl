<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>ProductActivationContact__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>Person who will be contacted in the event of issues during product activation.</description>
    <inlineHelpText>Person who will be contacted in the event of issues during product activation.</inlineHelpText>
    <label>Product Activation contact</label>
    <lookupFilter>
        <active>true</active>
        <errorMessage>The contact does not belong to the associated company for this partner integration.</errorMessage>
        <filterItems>
            <field>Contact.AccountId</field>
            <operation>equals</operation>
            <valueField>$Source.Id</valueField>
        </filterItems>
        <isOptional>false</isOptional>
    </lookupFilter>
    <referenceTo>Contact</referenceTo>
    <relationshipLabel>Accounts (Product Activation contact)</relationshipLabel>
    <relationshipName>AccountsProductActivationContact</relationshipName>
    <required>false</required>
    <trackFeedHistory>false</trackFeedHistory>
    <trackHistory>false</trackHistory>
    <type>Lookup</type>
</CustomField>
